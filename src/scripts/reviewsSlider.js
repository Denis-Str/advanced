import Vue from 'vue';
import { swiper, swiperSlide } from 'vue-awesome-swiper'


new Vue({
   el: '#reviews-component',
   template: '#reviews',
   components: {
      swiper,
      swiperSlide
   }
});